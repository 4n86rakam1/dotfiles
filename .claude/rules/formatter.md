# Formatter

コード作成後は以下を実行する。プロジェクトに設定ファイルがあればそちらに従う。

- Bash: `shellcheck script.sh`
- Python: `ruff format file.py`
- CSS / HTML / JSON / YAML / Markdown: `prettier --write file.ext`
- JavaScript / TypeScript: `prettier --write file.ts`
